// This file is kept for compatibility but the main layout 
// is now in [locale]/layout.tsx for internationalization
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
